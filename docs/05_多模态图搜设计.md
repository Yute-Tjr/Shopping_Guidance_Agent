# 05 · 多模态图搜设计（Phase 5）

> 配套主文档 `01_项目开发文档.md`。本篇聚焦 Phase 5「拍照找货」加分项：iOS 端拍照/选图 → 服务端 vision embedding → 与 Phase 4 结构化筛选融合的 RAG 检索 → 商品推荐流。
> 数据工程基础见 `02_数据工程与RAG设计.md`，后端 API / Agent 编排见 `03_后端API与Agent编排.md`，iOS 端结构见 `04_iOS客户端设计.md`，本文档不重复这些通用基础。

---

## 1. 范围与目标

### 1.1 课题对照

| docs/01 Phase 5 加分项 | 本期取舍 |
| --- | --- |
| 5A 业务闭环（购物车/下单） | ❌ 不做。工程量大且不增对话能力。 |
| **5B 多模态拍照找货** | ✅ **主线，纵向做深**。 |
| 5C 语音 / TTS | ❌ 不做。与 RAG 闭环关联弱（I/O 换皮）。 |
| 5D 工程性能（缓存/首 token/骨架屏） | ❌ 不做。100 件 demo 上看不出数据，反而显得凑。 |

原则承袭课题"做精一项胜过浅尝三项"。

### 1.2 Phase 5 目标

在 Phase 4（对话能力增强）之上叠加多模态输入路径，达成：

1. **iOS 端可选图/拍照**与文字输入并存，上传到服务端
2. **服务端用 vision embedding 把图编码为查询向量**，与 Phase 4 结构化筛选条件（价格 / 品牌排除 / 品类）协同检索
3. **图文矛盾 LLM 仲裁**：retrieve 层不做语义裁决
4. **任意一环失败均能退化为纯文本流**：拍照不是新的必经路径，是增强项
5. **15 条图搜评测黄金集**：四类指标全部达标（详见 §6.3）

---

## 2. 关键决策记录

四个核心 fork 的选型（brainstorming 阶段已与用户对齐）：

| 决策点 | 选型 | 替代方案与放弃理由 |
| --- | --- | --- |
| 输入形态 | **图 + 文混合** | 纯图搜逻辑最简，但与现有过滤能力脱节，看起来像两个独立功能 |
| 图向量入库 | **复用 `products_text` collection**，chunk_type='image' 标量隔离 | 另开 `products_image` collection 多一套 schema/seed/双路 retrieve，100 件规模过度设计 |
| 上传 API | **二段式 `/upload/image` → image_id → `/chat`** | 一段式 `/chat` multipart + SSE 混在一个路由别扭，上传失败要在 SSE 表达制造复杂度 |
| 图文融合检索 | **方案 A：单向量多模态编码 + Phase 4 scalar filter** | B 双向量并行检索 + 加权合并（200+ 行 + 权重玄学，100 件上看不出收益）；C 纯图搜再 text rerank（图未召回的永远进不来） |

**方案 A 的关键洞察**：Doubao `multimodal_embeddings.create()` 接口本来就接 `[{type:text}, {type:image}]` 一起 → 返回**单个 2048 维统一向量**，这是接口契约的目标用法。Phase 4 的 query_rewriter / structured_retriever / scalar filter 全部零侵入复用——只是 query_vector 由 multimodal embed 生成。

---

## 3. 架构与数据流

### 3.1 整体

```
┌─────────────────────────  iOS (PriceCat)  ─────────────────────────┐
│  ChatView 输入栏                                                    │
│  ┌─[📷]─[图缩略图×]─┬─────[文字输入]─────┬─[发送]─┐                   │
│                                                                    │
│  发送：① 若选图 → POST /upload/image (multipart) → {image_id}       │
│        ② POST /chat {message, image_id} (JSON)                    │
│        ③ 用户气泡：顶部缩略图 + 下方文字气泡                            │
└──────────────────────────────┬─────────────────────────────────────┘
                               │ SSE
┌──────────────────────────────▼─────────────────────────────────────┐
│  Server / FastAPI                                                  │
│                                                                    │
│  POST /upload/image                                                │
│  ├─ 校验：≤ 1MB / jpeg|png|webp / Pillow 可解码                      │
│  ├─ 落盘：data/uploads/<yyyymmdd>/<uuid>.jpg                        │
│  ├─ 同步算 vision embedding → ImageEmbedCache.put(image_id, vec)    │
│  └─ 返回 {image_id, preview_url}                                    │
│                                                                    │
│  POST /chat                                                        │
│   └─ orchestrator.handle(message, image_id?, history, summary)     │
│       ├─ image_id == None → Phase 4 recommend / compare / clarify  │
│       └─ image_id 非空 → MultimodalBranch.handle():                 │
│            1. query_vec = ImageEmbedCache.get(image_id)             │
│                          ?? embedder.embed_multimodal(img, text)    │
│            2. parsed = query_rewriter.parse(text, history, summary) │
│            3. filter_expr = build_filter(parsed)                    │
│                            AND chunk_type IN ("image","title")      │
│            4. hits = milvus.search(query_vec, filter_expr)          │
│            5. products = retriever._aggregate(hits)                 │
│            6. msgs = build_image_search_messages(...)               │
│            7. LLM 流式 → card_extractor → SSE 9 类事件                │
└────────────────────────────────────────────────────────────────────┘

┌──────────────────────  Vector Index (Phase 5 增量)  ───────────────┐
│  Phase 1 已有：title / description / faq / review chunks            │
│  Phase 5 增量：每件商品 +1 条 image chunk                             │
│     ├─ source_id  = "<pid>#image#0"                                │
│     ├─ chunk_type = "image"                                        │
│     ├─ text       = "<image_path>"   (占位，不进 prompt)             │
│     └─ vector     = embedder.embed_image(<pid>_live.jpg)           │
│  100 chunk 增量 → 总 1192 chunks                                    │
└────────────────────────────────────────────────────────────────────┘
```

### 3.2 关键设计要点

1. **共用 collection**：vision 模型对图文统一编码到同一 2048 维空间，标量字段 `chunk_type` 做隔离即可。
2. **检索路径不分裂**：multimodal branch 仍是**一次 milvus search**，query_vector 多模态编码、filter_expr 多带 `chunk_type IN ('image','title')`。
3. **结构化条件零侵入**：价格 / 品牌 / 品类筛选走 Phase 4 `query_rewriter.parse()`（图本身不携带价格信息，价格永远从文字抽）。
4. **`chunk_type IN ('image','title')` 的意义**：让图搜同时召回标题命中——例如上传"白色洗面奶"图 + 文字"洗面奶"，"洗面奶"靠 title chunk 召回，颜色靠 image chunk。
5. **图文矛盾不在 retrieve 层仲裁**：Prompt 加一条「图文矛盾以文字为准」由 LLM 处理。
6. **`ImageEmbedCache`**：内存 LRU（cap=100、TTL=30min），避免同一 image_id 在 multi-turn 里重复算 vision embedding。冷启动 / 缓存 miss 时从落盘图重算（~+500ms）。
7. **Phase 4 退化路径仍在**：image_id 为空时走原 recommend/compare/clarify——纯文本用户体验零变化。

---

## 4. 组件清单

### 4.1 新增文件

| 文件 | 职责 |
| --- | --- |
| `server/app/api/upload.py` | `POST /upload/image` 路由：multipart 接 → 校验（MIME / 大小 / 解码） → 落盘 → 同步算 vision embedding 缓存 → 返回 `{image_id, preview_url}`。**单一职责**：只管上传 + 缓存预算，不做检索。 |
| `server/app/rag/image_embed_cache.py` | 内存 LRU（cap=100、TTL=30min）。key=`image_id`，value=`(vec, image_path)`。`get()` miss 返回 None，由调用方决定怎么补。`asyncio.Lock` 包写。 |
| `server/app/agent/multimodal_branch.py` | orchestrator 的"图+文"分支处理器。`async def handle(message, image_id, history, summary) -> RetrieveResult`。**与 clarify_detector / compare_planner 平级**，不在 orchestrator 主文件里塞 if-else 大块。 |
| `server/scripts/build_image_index.py` | Phase 5 增量索引脚本：遍历 `ecommerce_agent_dataset/*/images/*_live.jpg` → embed → 写 milvus（chunk_type='image'）。**幂等**：先按 source_id 删旧再插，支持 `--rebuild`。 |
| `server/scripts/eval/image_queries.json` | 15 条评测黄金集：`{image_path, text?, expected_pids}`。四类（详 §6.3）。 |
| `server/scripts/eval_image_search.py` | 跑 Top-1/3/5 召回，按四类分组。复用 Phase 1 的评测骨架。 |
| `client/ShoppingGuide/Features/Chat/ImagePicker.swift` | `PhotosPicker` 封装 + 压缩到 ≤ 1 MB（短边 1600 上限） → 输出 `Data` 给上层。 |
| `client/ShoppingGuide/Services/UploadService.swift` | `async func upload(image: Data) -> String` 调 `/upload/image` 拿 image_id。与 `ChatService` 分文件——上传失败与 chat 失败错误处理独立。 |
| `server/tests/test_image_embed_cache.py` | LRU + TTL + 并发 put 单测。 |
| `server/tests/test_multimodal_branch.py` | 注入 fake embedder + fake retriever，验证 query_vector / filter_expr / 分支路由。 |
| `server/tests/test_upload_api.py` | TestClient：happy path / 413 / 415 / 422 / 缓存命中。 |

### 4.2 改动现有文件

| 文件 | 改动 |
| --- | --- |
| `server/app/rag/embedder.py` | 加 `embed_image(path: str) -> list[float]` 与 `embed_multimodal(*, text: str \| None, image_path: str \| None) -> list[float]`。底层都走 `multimodal_embeddings.create()`，差别只在 input list 内容。L2 归一化沿用。 |
| `server/app/rag/retriever.py` | **不改**。`search()` 已支持 `filter_expr`，`_aggregate` 已按 product_id 跨 chunk_type 聚合。 |
| `server/app/rag/chunker.py` | **不改**。image chunk 不走 chunker，由 build_image_index.py 直接产出。 |
| `server/app/rag/milvus_store.py` | schema 不动；如 `text` 字段长度限制 < 200 需放宽（image chunk 的 text 是路径）。 |
| `server/app/agent/orchestrator.py` | 入口加 `image_id` 可选参数。`handle()` 开头判定：`if image_id: return await self._multimodal_branch.handle(...)`。Phase 4 路径不变。 |
| `server/app/agent/prompts.py` | 新增 `build_image_search_messages(...)`。在 `_BASE_RULES` 之上追加："用户上传了一张图（路径仅供你定位是哪件商品的图），请按图相似 + 文字约束综合推荐；图文矛盾时以文字为准"。`_format_retrieved_block` 不变。 |
| `server/app/api/chat.py` | `ChatRequest` pydantic 加 `image_id: str \| None = None`，透传 orchestrator。 |
| `server/app/api/deps.py` | wire `ImageEmbedCache` + `MultimodalBranch` 实例。 |
| `server/app/main.py` | 注册 upload router。 |
| `client/ShoppingGuide/Features/Chat/ChatView.swift` | 输入栏左加相机按钮；选图后输入栏上方出现缩略图条 + 删除 ×；`sendMessage()` 改造：有图先 await upload 拿 image_id 再发 chat。 |
| `client/ShoppingGuide/Features/Chat/MessageBubble.swift` | 用户气泡支持顶部内嵌缩略图（圆角 8、最大 200×200、超出按短边裁居中）。 |
| `client/ShoppingGuide/Models/ChatMessage.swift` | 加可选 `localImageURL: URL?` / `remotePreviewURL: URL?`。 |
| `client/ShoppingGuide/Services/ChatService.swift` | `send(...)` 加可选 `image_id` 参数透传到 body。 |
| `README.md` | 加 Phase 5 章节：架构 / 增量索引产出 / API 形态 / iOS UI / 评测结果。 |

### 4.3 设计原则注脚

- **`multimodal_branch.py` 独立成文件**：避免 orchestrator.py 进一步膨胀。它和 `clarify_detector` / `compare_planner` 同层级——都是"特定意图的处理器"。
- **`ImageEmbedCache` 独立成模块**：要被 `upload.py` 和 `multimodal_branch.py` 同时依赖，放在 retriever.py 里两边 import 会交叉。
- **`build_image_index.py` 不并入 `build_index.py`**：Phase 5 索引行为是"增量补 image chunk"，分开方便单独跑 `--rebuild` 不影响文本 chunk。

---

## 5. 错误降级矩阵

| 失败点 | 检测 | 降级行为 | 用户体验 |
| --- | --- | --- | --- |
| 上传 MIME 不合法 | upload.py 校验 | HTTP 415 + `{error:"图片格式不支持"}` | iOS toast 提示重选 |
| 上传 > 1MB | upload.py 校验 | HTTP 413 | iOS toast；客户端在 ImagePicker 已先压缩，正常不触发 |
| 图解码失败 | Pillow open() except | HTTP 422 | iOS toast"图片无法读取" |
| Doubao vision 限流/超时 | tenacity 重试 3 次仍失败 | upload 返 503 + `{degraded:true, fallback_text_only:true}` | iOS 提示"图片识别繁忙，可用文字继续"——chat 仍能发（不带 image_id） |
| `ImageEmbedCache` miss（重启后） | get() 返回 None | multimodal_branch 兜底：读落盘图重 embed 一次（~+500ms） | 用户无感，首 token 略增 |
| 落盘图被清理（image_id 悬空） | open() 抛 FileNotFoundError | 退化到纯文本流，emit warning 事件 | iOS 提示"图片已失效，按文字处理"，仍正常出推荐 |
| Milvus search 空结果 | hits == [] | 走 Phase 4 现有 retriever.search(text only) 兜底 | 用户至少能看到文字命中结果，不会"上传图就空" |
| LLM 流式失败 | sse 异常 | Phase 2 已有降级：吐 Top-3 retrieved 卡片 | 与现有一致 |

**核心兜底原则**：图任意一环出错，**始终能退化为纯文本流**。绝不出现"上传图后整个对话卡死"。

---

## 6. 测试策略

### 6.1 单测（pytest，新增约 23 例）

- `test_embedder.py` +4：`embed_image()` 返 2048 维 L2 归一向量 / `embed_multimodal(text+image)` 与纯图分支数值接近（容差） / 图路径不存在抛 FileNotFoundError / 双 None ValueError
- `test_image_embed_cache.py` 4：put/get / TTL 过期 / LRU 容量驱逐 / 并发 put 不撕裂
- `test_upload_api.py` 6：happy / 415 / 413 / 422 / 缓存命中复用 / vision 限流 503
- `test_multimodal_branch.py` 5：图+文→正确 query_vec & filter_expr / 仅图（text=None）不抽过滤 / image_id miss 兜底重算 / filter_expr 含 chunk_type IN / 聚合后 product_id 唯一
- `test_prompts.py` +2：`build_image_search_messages` 含"图文矛盾以文字为准"指令 / image_path 不出现在 `<retrieved_products>` 块（防泄漏）
- `test_orchestrator.py` +2：image_id=None 走原 recommend / image_id 非空走 multimodal

### 6.2 Smoke / 集成

- `scripts/smoke_image_chat.sh`：固定测试图（`tests/fixtures/sample_sneaker.jpg`） → upload → chat → 断言收到 token + product_card 事件
- iOS 模拟器手动：选 `_live.jpg` 自身验证"上传商品 A 官方图 → Top-1 == A"

### 6.3 评测（决定 Phase 5 是否达标）

`image_queries.json` 15 条，**四类**：

| 类型 | n | 构造方式 | 达标线 |
| --- | --- | --- | --- |
| 同款识别 | 4 | 上传商品自身 `_live.jpg` → expected = 该商品 | Top-1 ≥ 90% |
| 相似款 | 5 | 上传 A 图 → expected = 同品类邻近品牌 ×2-3 | Top-3 ≥ 80% |
| 图+价格 | 3 | 图 + "1000 元以下" → expected 在同品类便宜段 | Top-3 ≥ 80% |
| 图+品牌排除 | 3 | 图 + "不要 X 品牌" → expected 在同品类剔除 X | Top-3 ≥ 80% |

`eval_image_search.py` 输出 Markdown 报告（参考 `phase4_eval_report.md` 格式）落到 `docs/phase5_eval_report.md`。

---

## 7. 验收清单（Phase 5 完成的硬条件）

- [ ] 100 张商品图全部入向量库；`milvus_store.count(chunk_type='image') == 100`
- [ ] `/upload/image` happy path 在 iOS 模拟器跑通：选图 → 收到 image_id
- [ ] `/chat` 带 image_id 跑通：iOS 用户气泡显示缩略图 + 文字 + 收到推荐卡片
- [ ] smoke 四条 case 全过：
  - 上传 `p_sneaker_005_live.jpg` 原图 → Top-1 == p_sneaker_005
  - 上传 `p_sneaker_005_live.jpg` + "1000 元以下的" → 命中全部 ≤ 1000
  - 上传 `p_beauty_010_live.jpg` + "不要兰蔻" → 命中无兰蔻
  - 上传图后**服务端重启** → 再发 chat 仍能正确推荐（验证缓存重算路径，不依赖内存状态）
- [ ] `eval_image_search.py` 跑出报告，四类指标全部达标
- [ ] server 单测全过（预期 +23 例，总数约 199）
- [ ] README 加 Phase 5 章节，含实测产出 + 评测表 + 已知限制

---

## 8. YAGNI 边界（明确不做）

- ❌ 图片 EXIF 旋转处理（iOS PhotosPicker 已自动正向）
- ❌ 图片 OCR 抽文字喂检索（vision embedding 本身已学到字形语义，纯 OCR 增益小且复杂）
- ❌ 多图上传（一次一张已够 demo）
- ❌ 上传图持久化超 24h（每日 cron 清理即可，Phase 6 再做）
- ❌ 历史图复用（"还按第 1 轮那张图"心智复杂，Phase 5 不接）
- ❌ compare 分支接图（demo 用例少，Phase 6 再说）

---

## 9. 与既有阶段的依赖关系

| 来源 | 依赖内容 |
| --- | --- |
| Phase 1 | `RagRetriever` / `ProductTextStore` / `embedder` 基础设施全套复用；image chunk 走同一 collection |
| Phase 2 | `ProductCardExtractor` / SSE 9 类事件 / orchestrator 降级路径 |
| Phase 3 | `MessageBubble` 拆分逻辑（用户气泡上嵌缩略图沿用现有渲染层） |
| Phase 4 | `query_rewriter` 抽结构化条件 / `structured_retriever` 的 filter_expr 构造逻辑 / `memory` 的 history+summary 透传 |

**没有任何 Phase 1-4 文件需要回退或重写**——Phase 5 的全部代码改动都是"增量补一条多模态分支"。
